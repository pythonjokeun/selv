[project]
name = "selfie"
version = "0.1.0"
description = "A Python decorator for logging attribute changes in classes"
readme = "README.md"
requires-python = ">=3.8"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=8.3.5",
    "ruff>=0.15.1",
]
